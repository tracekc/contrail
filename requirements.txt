requests>=2.31
python-dotenv>=1.0
anthropic>=0.40
openai>=1.40
playwright>=1.40
# Native (browser-free) frame renderer, used when RENDERER=native.
skia-python>=144.0
# Optional: free offline TTS (TTS_PROVIDER=local). Download a voice with:
#   python -m piper.download_voices en_GB-alan-medium --download-dir voices
piper-tts>=1.4
edge-tts>=6.0
